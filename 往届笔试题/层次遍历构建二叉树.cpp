/*
给定一棵二叉树，返回其右视图列表：即从二叉树右侧看过去，从上到下每层看到的最右边的值的数组。需要实现的函数头如下：
*/
#include<vector>
using namespace std;

vector<int> rightView(TreeNode* root);

class TreeNode {
	TreeNode *left, *right;
	int val;
};

vector<int> rightView(TreeNode* root) {

}

int main() {
	int a;
}